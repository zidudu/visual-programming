#include <iostream>
using namespace std;
int main(){

//클래스란 객체를 정의하는 틀 혹은 설계도로서 클래스에 멤버 변수와 멤버 함수를 선언한다.
//c++객체를 생성할 때 객체를 초기화수 있다. 클래스는 객체가 생성될때 자동으로 실행되는
//  생성자라는 특별한 멤버 함수를 통해 객체를 초기화한다.
	// 한 클래스에 여러 개의 생성자 둘 수 있으나, 이 중 하나만 실행된다.
	
	//생성자의 목적은 객체 생성될때 필요한 초기 작업 하기 위함임.
	//생성자 함수는 오직 한번만 실행됨
	//생성자 함수의 이름은 클래스 이름과 동일하게 작성되어야 함
	//생성자 함수의 원형에 리턴 타입 선언하지 않음

	//Circle coin;
	//객체의 초기화 작업이 코딩된 Circle를 타겟 생성자

	//Circle()생성자는 객체의 초기화를 다른 생성자에 위임한다고 해서 위임 생성자

	//기본생성자
	//클래스에 선언된 어떤 생성자도 없을 때 컴파일러가 자동으로 생성해주는 생성자
	//디폴트 생성자라고도 불림
	//컴파일러는 보이지 않게 기본 생성자를 삽입함

	//생성자가 하나라도 선언됬다면 컴파일러는 기본 생성자를 자동 삽입하지 않음


	//소멸자
	//소멸자는 객체가 소멸되는 시점에서 자동으로 호출되는 클래스의 멤버 함수이다.
	//소멸자의 이름은 클래스 이름 앞에 ~를 붙임


	//인라인 함수: 짧은 코드로 구성된 함수에 대해, 함수 호출 오버헤드로 인한 프로그램의 실행 속도 저하 
	//막기 위해 도입된 기능
	


}
